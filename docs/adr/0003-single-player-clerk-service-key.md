# Single-player hack auth and billing

Hack demo uses **Clerk** for login and a single **CHAR service `CURSOR_API_KEY`** against the builder's own GitHub. No multi-tenancy, no BYOK, no customer Cursor accounts. Cloud Apply targets repos the service Cursor account can already access.
